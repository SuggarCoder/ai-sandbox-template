"use client";

import { motion } from "framer-motion";

import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export const config = {
  title: "Sample Control Panel",
  description: "Reference thread page generated by template."
};

export default function SampleThreadPage() {
  return (
    <div className="space-y-6">
      <header className="space-y-2">
        <Badge>Thread Generated</Badge>
        <h1 className="text-3xl font-semibold tracking-tight">{config.title}</h1>
        <p className="text-sm text-muted-foreground">{config.description}</p>
      </header>
      <motion.div
        initial={{ opacity: 0, y: 8 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.2 }}
      >
        <Card>
          <CardHeader>
            <CardTitle>Control Surface</CardTitle>
            <CardDescription>Replace this file from your Slack bot implementation workflow.</CardDescription>
          </CardHeader>
          <CardContent className="flex flex-wrap gap-2">
            <Button>Primary Action</Button>
            <Button variant="secondary">Secondary</Button>
            <Button variant="outline">Outline</Button>
          </CardContent>
        </Card>
      </motion.div>
    </div>
  );
}

